
export const initialState = {
    error: '',
    isFetching: false,
    playerInfo: null,
} 

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}